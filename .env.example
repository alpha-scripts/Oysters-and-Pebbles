MONGODB_URI=mongodb+srv://USER:PASSWORD@CLUSTER.mongodb.net/opautos?retryWrites=true&w=majority

# Optional: only needed if your local network can't resolve mongodb+srv:// DNS
# SRV records (common with some VPNs/routers). Leave unset on Vercel.
# DNS_SERVERS=8.8.8.8,1.1.1.1
